<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ModelMeister.Ui.ViewModels"
             xmlns:views="using:ModelMeister.Ui.Views"
             x:Class="ModelMeister.Ui.Views.PersonalWorkAreaView"
             x:DataType="vm:PersonalWorkAreaViewModel">
  <!-- Reuses the shared Manage experience; the user-picker + personal scope come from the VM. -->
  <views:WorkAreaView />
</UserControl>
